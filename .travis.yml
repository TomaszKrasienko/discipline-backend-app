language: csharp
dotnet: 8.0
mono: none
sudo: required
dist: focal

branches:
  only:
    - release

services:
  - docker

before_script:
  - chmod -R a+x scripts

after_success:
  - ./scripts/dockerize.sh
